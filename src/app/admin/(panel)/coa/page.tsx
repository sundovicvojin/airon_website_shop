import { AdminEmpty, AdminPageHeader, StatusPill } from "@/features/admin/components/admin-ui";
import { getAdminBatches, getAdminCoa, getAdminProductsForSelect } from "@/features/admin/data";
import { updateCoaState } from "@/features/admin/entities/actions";
import { CoaForm } from "@/features/admin/entities/entity-forms";

export default async function CoaPage(){const[products,batches,documents]=await Promise.all([getAdminProductsForSelect(),getAdminBatches(),getAdminCoa()]);return <><AdminPageHeader description="Private-by-default certificate documents." title="COA documents"/><CoaForm batches={batches.map(({id,product_id,batch_number})=>({id,product_id,batch_number}))} products={products}/>{documents.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Title</th><th>Product</th><th>Batch</th><th>Visibility</th><th>Uploaded</th><th>Action</th></tr></thead><tbody>{documents.map(document=><tr key={document.id}><td>{document.title}</td><td>{document.products?.slug}</td><td>{document.product_batches?.batch_number??"General"}</td><td><StatusPill tone={document.public_visible&&document.active?"good":"neutral"}>{document.public_visible&&document.active?"PUBLIC":"PRIVATE"}</StatusPill></td><td>{new Date(document.uploaded_at).toLocaleDateString()}</td><td><form action={updateCoaState.bind(null,document.id,!document.public_visible,!document.active)}><button type="submit">{document.public_visible&&document.active?"Make private":"Publish"}</button></form></td></tr>)}</tbody></table></div>:<AdminEmpty>No COA documents yet.</AdminEmpty>}</>}
